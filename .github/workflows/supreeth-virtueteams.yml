name: Supreeth VirtuTeams Sign-In
on:
  schedule:
    # Runs every weekday (Monday-Friday) and Saturday at 9:20 AM IST (3:50 AM UTC)
    # Backup: Also runs at 9:22 AM IST (3:52 AM UTC) in case of delay
    - cron: '50 3 * * 1-6'
    - cron: '52 3 * * 1-6'
  workflow_dispatch:  # Allow manual trigger for testing

jobs:
  signin:
    runs-on: ubuntu-latest
    timeout-minutes: 10  # Set timeout to prevent hanging
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
          
      - name: Cache Playwright browsers
        uses: actions/cache@v3
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-playwright-
          
      - name: Install Playwright
        run: |
          pip install playwright
          playwright install chromium --with-deps
          
      - name: Run Supreeth Sign-In
        run: |
          echo "🕐 Starting Supreeth's VirtuTeams automation at $(TZ='Asia/Kolkata' date)"
          python3 supreeth_virtueteams.py
          echo "✅ Supreeth's automation completed at $(TZ='Asia/Kolkata' date)"
        
      - name: Upload screenshots as artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: supreeth-screenshots
          path: |
            *.png
          retention-days: 7

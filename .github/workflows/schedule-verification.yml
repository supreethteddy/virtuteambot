name: Schedule Verification
on:
  schedule:
    # Runs every weekday (Monday-Friday) at 9:25 AM IST (3:55 AM UTC) to verify schedules
    - cron: '55 3 * * 1-5'
  workflow_dispatch:  # Allow manual trigger for testing

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check Schedule Status
        run: |
          echo "🔍 Verifying VirtuTeams Automation Schedules"
          echo "=========================================="
          echo ""
          echo "Current UTC time: $(date -u)"
          echo "Current IST time: $(TZ='Asia/Kolkata' date)"
          echo ""
          echo "📅 Scheduled Times:"
          echo "• Supreeth: 9:30 AM IST (4:00 AM UTC)"
          echo "• Kavya: 9:35 AM IST (4:05 AM UTC)"
          echo ""
          echo "✅ Schedule verification completed"
          echo "Both workflows should run automatically in the next 10 minutes"
